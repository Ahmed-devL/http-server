# Tiny HTTP Server

A minimal HTTP server built in C++ using POSIX sockets. Written as a learning project to understand how networking and the internet actually work.

(More details coming after testing...)